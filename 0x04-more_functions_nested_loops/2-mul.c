#include "main.h"
#include <stdio.h>

/**
 * mul - a function that multiplies two integers.
 * @a,b:characters to be checked
 *
 * Return: Always 0
 */

int mul(int a, int b)
{
	mult = a*b;
	return (mult);
}
